# Printer-Queue-DSA-02
In this assignment, we built a Printer Queue System in C++ using FIFO rule. Each print job has a Job ID and page count. We implemented enqueue, dequeue, peek, display, and totalPages operations. Five print jobs were tested by adding, removing, and displaying them using a linked list based queue.
